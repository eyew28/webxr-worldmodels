import { buildSophieChatPanel } from "./sophieChat.js";

/** Returns the Sophie panel content element (positioning handled by toolbar). */
export function buildSophiePanel(): HTMLElement {
  return buildSophieChatPanel();
}
